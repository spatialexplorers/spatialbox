{
  "name": "@spatialbox/theming",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "copy-tokens": "sbox theme tokens ./theme",
    "generate-theme": "sbox theme build ./theme",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "dependencies": {
    "@spatialbox/cli": "*"
  },
  "devDependencies": {
    "@spatialbox/tsconfig": "*",
    "eslint-config-spatialbox": "*",
    "typescript": "^4.7.4"
  }
}
